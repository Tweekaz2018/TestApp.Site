@model IEnumerable<TestApp.Domain.Category> 
@{
    Layout = null;
}
<div class="col-2">
    @foreach (var cat in Model)
    {
            <a asp-controller="Shop" asp-action="Index" asp-route-id="@cat.Id" class="btn-block btn btn-outline-dark">@cat.name</a>
    }
</div>
